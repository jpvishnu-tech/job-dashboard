/**
 * firebase-config.js — DEPRECATED
 * Firebase has been replaced by Supabase. See src/lib/supabase.js.
 *
 * This stub is kept so that any stale imports do not cause build errors.
 * Remove it once you have confirmed no other file imports from here.
 */

export const isFirebaseConfigured = false;
export const auth = null;
